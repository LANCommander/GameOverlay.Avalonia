<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <ImplicitUsings>disable</ImplicitUsings>
    <!-- Must match the library's InternalsVisibleTo so the test can drive the
         internal OverlaySession + TestPatternFrameSource directly. -->
    <AssemblyName>GameOverlay.Linux.TestHost</AssemblyName>
    <RootNamespace>GameOverlay.Linux.TestHost</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\..\src\GameOverlay.Avalonia\GameOverlay.Avalonia.csproj" />
  </ItemGroup>

</Project>
